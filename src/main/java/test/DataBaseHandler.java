package test;

import org.apache.commons.lang3.ClassUtils;

import java.lang.reflect.Method;

public class DataBaseHandler {

    /*public <T> T build(Class<T> handlerObject){

        Object obj = new Object();

        Class aClass = obj.getClass().asSubclass(handlerObject);


        for (Method method : handlerObject.getDeclaredMethods()){
            if (AnnitatoinProccessor.isDao(method.getReturnType())){

                for (Method queryMethod : method.getReturnType().getDeclaredMethods()){
                    if (AnnitatoinProccessor.isQuery(queryMethod)){

                    }
                }
            }
        }

    }*/


}
